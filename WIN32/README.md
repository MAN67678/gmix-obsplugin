# gmix-obsplugin (Windows) — not coming soon

Placeholder. The Windows port of the zero-copy OBS plugin architecture
(see `../linux-x86_64/`) would live here, but there's no active work on it
and no timeline. The blocker is the developer's own hardware/driver setup
(no Windows machine with a suitable GPU/driver combo to build and test the
Vulkan capture + zero-copy dma-buf path against), not a decision against
Windows itself — GMix is Linux-exclusive for now. This'll be revisited if/
when the hardware situation changes.
