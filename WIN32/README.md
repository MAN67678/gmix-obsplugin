# gmix-obsplugin (Windows) — not yet ported

Placeholder. The Windows port of the zero-copy OBS plugin architecture
(see `../linux-x86_64/`) will live here. System requirements will be listed
in the top-level README once this exists.
